# プロジェクト: ピップソケット・クロニクル

## 変更後の自動デプロイ
コード変更が完了したら、以下を自動で実行する:
1. `git add` で変更ファイルをステージ
2. `git commit` で変更内容を日本語でコミット
3. `git push` でGitHubにプッシュ
4. `npm run deploy` でGitHub Pagesにデプロイ

## 技術スタック
- Vite + React + TypeScript
- Zustand (状態管理)
- Dexie.js (IndexedDB保存)
- GitHub Pages (デプロイ)

## カラーパレット
- 白〜ベージュ背景 (#f5f0e8, #ece5d8)
- テキスト茶系 (#3a2a1a, #6a5a4a, #998a78)
- ネオン・グロー・シャドウ禁止
